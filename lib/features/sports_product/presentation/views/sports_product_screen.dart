import 'package:flutter/material.dart';

class SportsProductScreen extends StatelessWidget {
  const SportsProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
